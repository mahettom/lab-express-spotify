 {{!-- Pour chaque artist --}}
{{#each artistArray}}

{{!-- créer un div contenant "l'image" --}}
<div class="artist-container">

    <img src="{{images.0.url}}" alt="image-artist">

{{!-- puis un autre div contenant "nom, lien vers albums" --}}
    <div class="artist-info">
        <h1>{{name}}</h1>
        <a href="/albums/{{id}}">show {{name}} albums</a>
    </div>
</div>

{{/each}}
